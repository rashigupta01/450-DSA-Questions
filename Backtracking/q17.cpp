// Simple reccursion question

// add the current element to the path then reccurse with downward and rightward movement if possible. Print path once bottom right
// corner is reached. backtrack by removing the current element from path. 
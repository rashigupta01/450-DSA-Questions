// Singly linked list can't be reversed in less than O(n)
// Doubly linked list also can't be reversed in less than O(n) because if we just reverse head and tail
// then we will have to traverse in forward direction using prev and in backward using next